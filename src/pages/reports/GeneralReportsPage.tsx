import { GeneralStudio } from "@/components/reports/GeneralStudio";

/**
 * HADRAN FABRICS MALL — General Reports & Analysis Studio (Phase 9).
 * 12 general report types + 8 general analysis types that merge records
 * across all business sections and branches into unified, exportable
 * results. Requires the reports.general permission (Admin and above).
 */
export default function GeneralReportsPage() {
  return <GeneralStudio />;
}
