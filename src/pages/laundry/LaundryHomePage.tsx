import { SectionHomePage } from "../sections/SectionHomePage";

/** Laundry section home — headline numbers + quick links. */
export default function LaundryHomePage() {
  return <SectionHomePage section="LAUNDRY" />;
}
