import { SectionHomePage } from "../sections/SectionHomePage";

/** Tailoring section home — headline numbers + quick links. */
export default function TailoringHomePage() {
  return <SectionHomePage section="TAILORING" />;
}
